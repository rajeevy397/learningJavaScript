const defaultResult = 0;
var currentResult = defaultResult;

currentResult = (currentResult + 10) * 3 / 2 - 1;

let calcuationDescription = `(${defaultResult} +10) * 3 / 2 - 1`;

outputResult(currentResult, calcuationDescription);
